# SalesIntelligence + CV Quality Web App – Render Dependencies

# Web Framework
flask>=3.0.0
gunicorn>=22.0.0

# CV Quality – Anomaly Detection (PatchCore + FoundAD)
# NOTE: torch + torchvision are installed CPU-only in render.yaml buildCommand
# (pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cpu)
# Do NOT add them here again to avoid re-downloading the CUDA build.
Pillow>=10.0.0
scikit-image>=0.22.0

# RunPod API Client
requests>=2.31.0

# Daten
numpy>=1.26.0
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.1

# Graph ML
networkx>=3.2
scipy>=1.11.0

# Demand Forecasting
scikit-learn>=1.3.0
lightgbm>=4.3.0

# Utilities
tqdm
